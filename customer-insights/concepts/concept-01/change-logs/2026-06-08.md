# 2026-06-08

## What was reworked
- Restarted the implementation pass for the clickable `Cohort per ASIN` prototype.
- Renamed the HTML artifact to `design-iteration-01.html` so the concept folder reflects the iteration naming used elsewhere.
- Reconfirmed the shell should be rebuilt from the linked `Customer Insights - Omny` Figma source instead of the earlier HTML mock.
- Restated the tooltip changes:
  - `Product Role` legend/list moves into the `Product Role` column-header tooltip.
  - `LTV Breakdown` legend/list becomes a tooltip pattern instead of a visible header legend block.
- Re-centered the plan around the module-level shell details from `module-brief.md` and the concept-specific `Product Role` copy, badge colors, and hover explanations.
- Added a real iteration folder for Concept 1 with a single prototype page that exposes three visual directions through a bottom-right pill switcher.
- Grounded the page shell in the module brief by placing the global filters, tab strip, and currency selector in the top shell area.
- Added hover states for both `Product Role` and each `LTV Breakdown` cell, with the breakdown legend moved out of the header and into the cell tooltip.
- Reworked the shell to match the reference screenshot more closely by moving the filters into the global header and moving the metric selector into the table-header zone.
- Removed clipping constraints from the shell containers so the tooltips can overflow freely and stay fully visible.
- Split the `Product Role` badge tooltip into a short description plus a computation line so the badge hover now mirrors the requested annotation feedback.
- Made iterations `02` and `03` visually distinct in the `Product Role` column instead of only tinting table rows, and rebuilt the `LTV Breakdown` hover card into a structured three-line tooltip with title, summary, and per-component values.
- Replaced the left rail letter placeholders and simplified the top shell so the navigation, tab strip, and global header read closer to the client-facing Figma reference.
- Consolidated the product thumbnail, product name, and ASIN into a single leading identity column so the table matches the reference layout more closely.
- Made the metric selector clickable, removed iteration `02` from the switcher, tightened the table typography to the requested 14px/13px hierarchy, and widened the v3 role column so its badges are not truncated.
- Removed the Product Role tooltip width cap so it hugs its content, and labeled the v3 dot meter as the three-part LTV balance signal.
- Renamed the dot-led Product Role iteration from `03` to `02`, kept the three breakdown dots but aligned their colors to the breakdown categories, removed the pill treatment from that variant, and stacked the tag copy beneath the dots so the column can stay narrower.
- Refined the `02` role column again so the dots are color-matched to the role signal, the in-cell tag description is removed, and the entire role cell is left unshaded for a lighter column treatment.

## Current status
- The prototype frame already exists in the Figma file, but the connector requires reauthentication before further write/read actions can succeed.
- Once the connection is restored, the next step is to:
  - finish the shell alignment against the source frame,
  - wire the tooltip prototype states,
  - and validate the clickable flow.

## Notes
- The concept-specific design-plan file remains the planning source for the implementation pass.
- No concept content decisions changed; this pass is about rebuilding the shell and interaction pattern more faithfully.
