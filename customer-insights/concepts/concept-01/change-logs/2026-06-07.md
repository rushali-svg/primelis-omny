# 2026-06-07

## What was worked on
- Created the `concept-01` scaffold for the Product Role Column concept.
- Added concept-specific docs for the user flow, use cases, references, and concept brief.
- Added the initial metadata file for the concept.
- Implemented the first HTML/CSS design pass for the `Cohort per ASIN` tab shell.
- Added a written design plan for the first design pass.

## What changed
- The concept now has its own folder instead of relying only on the global `context/` docs.
- The concept docs separate the new tagging behavior from the module-wide Customer Insights context.
- The project now has a canonical `TAGS.md` for concept metadata.
- The concept folder now contains a static design artifact that reflects the module chrome, the reference table structure, and the `Product Role` interaction layer.

## Decisions locked in
- `Product Role` is a concept-specific behavior for the `Cohort per ASIN` tab, not a global Customer Insights rule.
- This concept needs its own `userflow.md` and `use-cases.md` because it adds new interaction patterns and edge cases.
- The future role filter chip is out of scope for the first concept iteration.

## New plan for the Figma build
- Rebuild the shell directly from the linked Customer Insights Figma file instead of using the HTML mockup as the source.
- Keep the page shell, navigation, and table hierarchy aligned to the existing Figma structure.
- Move the `Product Role` legend into the `Product Role` column header as a tooltip interaction.
- Remove the `LTV Breakdown` legend from the header and make it a tooltip pattern instead.
- Add clickable prototype behavior so the first pass can be reviewed interactively inside Figma.
